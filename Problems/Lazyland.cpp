#include<bits/stdc++.h>

using namespace std;

int main()
{
	/* code */
	int n,k;
	cin >> n >> k;
	int jobs[n+10], min[n+10];
	for (int i = 0; i < n; ++i)
	{
		/* code */
		scanf("%d", &jobs[i]);
	}
	for (int i = 0; i < n; ++i)
	{
		/* code */
		scanf("%d", &min[i]);
	}
	return 0;
}